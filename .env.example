# Copia questo file in .env e compila i valori:
#   cp .env.example .env && chmod 600 .env
#
# Database (backup-db.sh)
DB_BACKUP_USER=
DB_BACKUP_PASSWORD=

# Percorsi locali
BACKUP_DB_DIR=/home/ubuntu/backup-db
BACKUP_FILES_ROOT=/home/ubuntu/backup
SOURCE_WEB_DIR=/var/www/html
WEB_ROOT=/var/www
ERROR_LOG_BACKUP_FILES=/home/ubuntu/errors_backup_files.log

# Rclone (nome remote e percorsi sul cloud)
RCLONE_REMOTE_DB=gdrive:backup-mia-vps/db
RCLONE_REMOTE_FILES=gdrive:backup-mia-vps/files

# Telegram (backup-files.sh)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Retenzione: directory locali separate da spazio
RETENTION_LOCAL_DIRS="/home/ubuntu/backup /home/ubuntu/backup-db"
# Retenzione remota: percorsi rclone separati da spazio
RETENTION_REMOTE_DIRS="gdrive:backup-mia-vps/files gdrive:backup-mia-vps/db"
